# Admin
pip-tools
fabric3             # project automation
tqdm                # script progress bars

# Celery
celery[redis]       # task queue
flower              # monitoring
watchdog            # restart celery in dev (via 'watchmedo' command)
argh                # needed by watchdog
pyyaml              # needed by watchdog

# Database
psycopg2            # postgres connector

# Django Stuff
django
django-extensions
django-filter
djangorestframework
whitenoise          # static file serving

# Features
ua-parser           # user-agent parsing

# API
requests            # need for contacting api server

# Testing
flake8
pytest
pytest-django
pytest-cov          # record code coverage
pytest-mock         # mocking
factory-boy         # create django model fixtures on demand
pyhumps             # a helper for dealing with camel case and snake case
